# The BUNDLE IS THE PRODUCT here, so dist/ is deliberately NOT ignored.
# index.html is generated by tools/build-bundle.mjs and is committed for the same reason:
# somebody who downloads this repository must be able to double-click it.

node_modules/
*.log
.DS_Store
Thumbs.db

# Scratch output from the proof harnesses
.refs-tmp/
*.tmp.json

# design-lint hook cache, not source
.impeccable/

# One-time internal licence migration tool; public tree is already BUSL-1.1.
tools/relicense-to-bsl.mjs
